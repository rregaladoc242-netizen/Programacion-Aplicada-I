<Window x:Class="EjemploMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EjemploMVVM"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="850">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Controles superiores: Categoría, Búsqueda y Botón centrados -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,15">
            <TextBlock Text="Categoría:" Margin="0,0,5,0" VerticalAlignment="Center"/>
            <ComboBox Width="150" Margin="0,0,15,0" 
                      ItemsSource="{Binding Categorias}" 
                      DisplayMemberPath="NombreCategoria" 
                      SelectedValuePath="IdCategoria"
                      SelectedValue="{Binding CategoriaSeleccionada}"/>

            <TextBlock Text="Buscar:" Margin="0,0,5,0" VerticalAlignment="Center"/>
            <TextBox Width="180" Margin="0,0,10,0" VerticalContentAlignment="Center" Text="{Binding textoBuscar, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Buscar Productos" Padding="8,4" 
                    Command="{Binding ComandoCargarProductos}"/>
        </StackPanel>

        <!-- Tabla de Productos -->
        <DataGrid Grid.Row="1" IsReadOnly="True" ItemsSource="{Binding productos}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="ID" Width="70"/>
                <DataGridTextColumn Binding="{Binding nombre}" Header="Nombre Producto" Width="320"/>
                <DataGridTextColumn Binding="{Binding precio}" Header="Precio" Width="100"/>
                <DataGridTextColumn Binding="{Binding discontinuado}" Header="Discontinuado" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>